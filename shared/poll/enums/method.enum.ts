export enum PollMethod {
    RUNOFF = "runoff",
    POSITIONAL = "positional",
    SCORE = "score",
    APPROVAL = "approval",
    PLURALITY = "plurality",
}

export enum VoterIdentityMethod {
    IP = "ip",
    COOKIE = "cookie",
}
