#!/usr/bin/env bash
set -e

docker compose -f compose.yml down -v --remove-orphans 2>/dev/null || true
docker compose -f compose.production.yml down -v --remove-orphans 2>/dev/null || true
docker compose -f compose.test.yml down -v --remove-orphans 2>/dev/null || true


echo "All docker containers stopped"
