#!/usr/bin/env bash

if [ "$1" = "headless" ]; then
  shift
  docker compose run --rm playwright npx playwright test "$@"
else
  docker compose run --rm playwright npx playwright test --ui "$@"
fi
#!/usr/bin/env bash

docker compose exec webserver npx drizzle-kit generate