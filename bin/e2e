#!/usr/bin/env bash

if [ "$1" = "headless" ]; then
  shift
  docker compose run --rm playwright npx playwright test "$@"
else
  docker compose run --rm playwright npx playwright test --ui-host=0.0.0.0 --ui-port=8080 "$@"
fi
