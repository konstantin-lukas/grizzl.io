#!/usr/bin/env bash

if [ "$1" ]; then
  docker compose run --rm -e CI=true playwright npx playwright test -u missing "$1"
else
  docker compose run --rm -e CI=true playwright npx playwright test -u missing
fi
